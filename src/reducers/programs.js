const initPrograms = {
    '1': {
        id: '1',
        team: 'A',
        instructionList: ['1', '2', '3']
    }
}

export const programs = ( state=initPrograms, action ) => {
    switch (action.type) {
        default:
            return state
    }
}
